import { coldQuestion } from 'codereview/helpers/cold-question';
import { module, test } from 'qunit';

module('Unit | Helper | cold question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coldQuestion([42]);
  assert.ok(result);
});
