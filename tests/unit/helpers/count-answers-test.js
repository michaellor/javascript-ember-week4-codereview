import { countAnswers } from 'codereview/helpers/count-answers';
import { module, test } from 'qunit';

module('Unit | Helper | count answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = countAnswers([42]);
  assert.ok(result);
});
