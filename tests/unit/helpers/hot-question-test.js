import { hotQuestion } from 'codereview/helpers/hot-question';
import { module, test } from 'qunit';

module('Unit | Helper | hot question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hotQuestion([42]);
  assert.ok(result);
});
